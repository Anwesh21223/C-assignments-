#include<stdio.h>
#include<math.h>
int main()
{
    int n,i; int b;
    printf(" Enter a number:");
    scanf("%d",&n);
    for(i=1;i<n;i++){
        if(i*i<=n ){
            if((i*i)%2==0)
        printf("\n%d",i*i);}
        }
    }
