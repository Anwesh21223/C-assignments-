Context: The questions in this folder moves from basic input output plus calculations from eariler folder to now understanding the same programs to a much 
         deeper level like understaning the standard printf and scanf functions, their differences and conversion specification. We also get to know more 
         in detail about format strings and how the compiler actually stores the output inside the computer in low level
         by reading the program word for word. And how the compiler expects somekind of things while checking for the input and how it store random 
         garbage value in the memory if it gets somethig that was not expected according to earlier statements.
